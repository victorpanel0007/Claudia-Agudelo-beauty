import ComisionesView from '@/components/admin/ComisionesView'
export const metadata = { title: 'Comisiones y Pagos — Admin' }
export default function ComisionesPage() {
  return <ComisionesView />
}
