import RecordatoriosConfig from '@/components/admin/RecordatoriosConfig'

export const metadata = { title: 'Recordatorios — Admin' }

export default function RecordatoriosPage() {
  return <RecordatoriosConfig />
}
